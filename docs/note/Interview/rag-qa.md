# RAG 设计问答

## Q：RAG 和微调的区别是什么？

RAG 通过检索外部知识来增强模型回答，不需要修改模型参数。微调需要训练模型，成本更高。RAG 适合知识频繁更新的场景。

## Q：如何设计分块策略？

需要平衡块大小和语义完整性。太小会丢失上下文，太大会引入噪音。常用方法包括按段落分块、按语义分块、滑动窗口。

## Q：如何评估 RAG 效果？

核心指标包括检索准确率、回答相关性、幻觉率。可以通过人工评估和自动评估结合。

## Q：如何处理检索不到相关内容的情况？

需要设计兜底策略，比如返回"无法回答"、引导用户换一种问法、或者降级到通用回答。

待继续补充。
